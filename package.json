{
  "name": "ridge-trail-passport",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.16 <23"
  },
  "scripts": {
    "dev": "vite",
    "test": "node --test",
    "prebuild": "npm test",
    "build": "vite build",
    "check": "npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "leaflet": "1.9.4"
  },
  "devDependencies": {
    "vite": "8.2.2"
  }
}
